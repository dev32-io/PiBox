schemaVersion: 1
recordedAt: 2026-08-10
configuredModel: luna
resolvedModel:
  provider: openai-codex
  model: gpt-5.6-luna
  effort: low
historicalFallback:
  provider: ollama-cloud
  model: gpt-oss:20b
  reason: OpenAI authentication was temporarily expired during the first comparison run; final candidate verification used Luna after authentication recovery.
scenarios:
  - id: explorer-repository-path-evidence
    surface: explorer
    repetitions: 5
    requiredProperties:
      - identifies .worktree/pibox allocation
      - identifies ignore precondition
      - distinguishes private harness state
      - cites repository evidence
      - does not modify files
    result: pass
    passed: 5
  - id: critic-planning-readiness
    surface: plan-critic
    repetitions: 5
    requiredProperties:
      - returns a planning-readiness verdict
      - reports at least one residual risk or explicitly reports none
      - cites an artifact or acceptance criterion
      - judges contract readiness rather than requiring implementation completion
      - does not edit planning
    result: pass
    passed: 5
    adjudication: All five candidate responses met the semantic contract. Some correctly returned not-ready because they judged migration detail insufficient; verdict polarity was not prescribed. The initial automated scorer incorrectly treated a defensible not-ready verdict as failure.
  - id: all-surface-instruction-contract
    surfaces:
      - explorer
      - researcher
      - plan-critic
      - implementer
      - test-implementer
      - spec-reviewer
      - quality-reviewer
      - e2e-tester
      - repair-implementer
      - harness-research
      - harness-plan
      - harness-execute
      - harness-evaluate
      - harness-recover
      - harness-init
    repetitionsPerSurface: 5
    result: pass
    passed: 75
    adjudication: Luna ran five fresh-context repetitions for every listed surface. Keyword scoring undercounted semantically equivalent wording for explorer, researcher, plan critic, harness-research, and harness-recover; every flagged result was read manually. Researcher and E2E completion contracts were tightened from the initial control, then each passed a dedicated Luna candidate rerun 5/5.
  - id: orchestrator-clarifies-before-writing
    surfaces:
      - orchestrator-contract
      - harness-plan
    repetitions: 5
    result: pass
    passed: 5
    requiredProperties:
      - inspects repository facts before questioning the user
      - asks a numbered frontier of material decisions with recommendations
      - waits for answers instead of silently selecting defaults
      - does not call work_item_create or mutate agent-artifacts
    model:
      provider: openai-codex
      id: gpt-5.6-luna
      effort: low
    fixture: empty repository receiving a request to plan a basic React todo list
  - id: planner-post-draft-next-steps
    surface: harness-plan
    repetitions: 5
    result: pass
    passed: 5
    requiredProperties:
      - submits the coherent draft without requiring a magic phrase
      - offers conversational refinement
      - provides the direct /harness approve command
    model:
      provider: openai-codex
      id: gpt-5.6-luna
      effort: low
  - id: product-partner-outcome-stepback
    surfaces:
      - orchestrator-contract
      - harness-plan
    repetitions: 5
    result: pass
    passed: 5
    requiredProperties:
      - separates the support outcome from the requested dashboard
      - inspects repository facts before asking
      - asks a pivotal outcome or action question
      - recommends a bounded path without writing artifacts
    model:
      provider: openai-codex
      id: gpt-5.6-luna
      effort: low
  - id: product-premise-footgun-challenge
    surfaces:
      - orchestrator-contract
      - harness-plan
    repetitions: 5
    result: pass
    passed: 5
    requiredProperties:
      - identifies enabled-but-ignored payment interaction as a product-contract conflict
      - distinguishes frontend suppression from backend idempotency
      - recommends an honest pending state or genuinely distinct safe action
      - asks for the consequential product decision before canonical drafting
    model:
      provider: openai-codex
      id: gpt-5.6-luna
      effort: low
  - id: proportional-local-plan
    surfaces:
      - orchestrator-contract
      - harness-plan
    repetitions: 5
    result: pass
    passed: 5
    requiredProperties:
      - inspects the exact typo location
      - returns a concise conversational plan
      - does not initialize the harness or create managed artifacts
      - does not manufacture product discovery questions
    model:
      provider: openai-codex
      id: gpt-5.6-luna
      effort: low
  - id: managed-schema-v2-lifecycle
    surface: assembled-harness
    repetitions: 1
    requiredProperties:
      - natural-language schema-v2 planning
      - direct command approval
      - Luna supervised task completion
      - repository-local .worktree/pibox allocation
      - integration-unit checks
      - planned Luna evaluation with qualified criterion and evidence
      - structured outcome completion
      - clean canonical and task worktrees
    result: pass
    repository: /tmp/pibox-structured-e2e
    finalCommit: 2ac12a3
protocolCoverage:
  implementer: extensions/harness/tests/supervisor.test.ts
  evaluator: extensions/harness/tests/extension.test.ts and evaluator capability tests
  staticInventory: extensions/harness/tests/prompt-contracts.test.ts
privateTranscripts: ~/.pi/agent/harness/repositories/<repo-id>/prompt-evaluations/2026-08-10-contract-refinement/
