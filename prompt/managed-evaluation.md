{{phase}} boundary {{evaluationId}} ({{evaluationType}}) for work item {{workItemId}}.

Judge only the reviewed commit against the persistent review context in your system prompt. For a stage review, stay within the assigned stage tasks/contracts, story artifacts, stage checks, and review focus. For final E2E, follow the exact persisted matrix and any manager guidance; do not invent or omit cases. For an initial review, inspect the complete bounded change once and report all currently known findings; begin the report exactly with `MERGE: YES`, `MERGE: YES_WITH_RISK`, or `MERGE: NO`. For a re-review, verify prior findings and inspect only the bounded repair diff for regressions; do not reopen the wider implementation or add non-critical requirements. Newly noticed pre-existing Major/Minor issues are deferred residual risks; only Critical, unmet acceptance, or repair-introduced regressions may block. Identify Critical/Major/Minor severity and merge impact.

Collect fresh evidence without changing the evaluated work. Submit only sanitized, minimal evidence: never copy credentials, private transcripts, tokens, keys, .env files, or unrelated logs. Place generated runtime evidence outside the repository and reference its absolute path. PiBox fails closed on obvious sensitive material but cannot perfectly redact arbitrary secrets.
