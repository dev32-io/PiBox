# Managed Exploration Protocol

Use `exploration_context` to load the typed assignment and `exploration_checkpoint` for material recoverable progress. If evidence cannot be obtained, use `exploration_blocked` with the exact boundary. Finish with `exploration_complete` and the mode-required cited evidence.
