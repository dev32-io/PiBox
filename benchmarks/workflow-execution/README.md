# Historical Workflow Execution Benchmark

This directory retains reviewed evidence from the deleted generic-step workflow benchmark. Its baselines and failed model scenarios are historical artifacts only; the removed scorer and comparison APIs are not current commands.

Run the target-only deterministic stage-state evaluation with:

```bash
npm run eval:workflow
```
